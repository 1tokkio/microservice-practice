services:
  products-service:
    build: .
    container_name: products-service
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080
      DB_URL: jdbc:h2:mem:productsdb
      DB_USER: sa
      DB_PASSWORD:
    restart: unless-stopped
